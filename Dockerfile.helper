FROM python:3.12-slim

WORKDIR /app

# Copy everything from build context
COPY . .

# Install Helper dependencies (also installs discord.py for main imports)
RUN pip install --no-cache-dir -r Helper/requirements.txt

# Run Helper bot
CMD ["python", "Helper/main.py"]


