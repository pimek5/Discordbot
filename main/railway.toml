[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# Długi timeout dla inicjalizacji
healthcheckTimeout = 300

# Dłuższy grace period na shutdown
stopSignal = "SIGTERM"
