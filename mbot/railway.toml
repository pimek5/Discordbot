providers = ["nixpacks"]

[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["...", "ffmpeg"]

[deploy]
startCommand = "python mbot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[environmentVariables]]
name = "PYTHONUNBUFFERED"
value = "1"
